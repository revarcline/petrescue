<h1>All Users</h1>

<% @users.each do |user| %>
  <div class="user_link">
    <%= link_to user.full_name, user_path(user) %><%= " (admin)" if user.admin %>
  </div>
<% end %>

<h3>Set Admin</h3>

<h3>Revoke Admin</h3>

<h3>Delete Account</h3>
